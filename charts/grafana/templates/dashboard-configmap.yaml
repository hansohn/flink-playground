apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-flink
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/instance: {{ .Release.Name }}
    grafana_dashboard: "1"
data:
  flink-memory-monitoring.json: |-
{{ .Files.Get "dashboards/flink-memory-monitoring.json" | indent 4 }}
